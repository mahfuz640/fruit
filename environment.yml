name: fruit-yolo-opencv
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - --extra-index-url https://download.pytorch.org/whl/cpu
      - torch==2.5.1+cpu
      - torchvision==0.20.1+cpu
      - ultralytics>=8.3,<8.5
      - opencv-python-headless>=4.10
      - streamlit>=1.40
      - streamlit-webrtc>=0.47
      - av>=12
      - pillow>=10
      - onnx>=1.16
